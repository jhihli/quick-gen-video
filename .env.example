# TKVGen Environment Variables Template
# Copy this file to create your environment configuration:
# For development: cp .env.example .env.local
# For production: cp .env.example .env.production

# Development Port Configuration
# Frontend (Vite) port
VITE_PORT=3002
# Backend (Express) port
SERVER_PORT=5003

# Legacy port support (for production deployment compatibility)
PORT=5003

# Base URLs for frontend and backend
# Development example:
REACT_APP_BASE_URL=http://localhost:5003
BASE_URL=http://localhost:5003

# Production example (replace with your actual domain):
# REACT_APP_BASE_URL=https://wgenv.com
# BASE_URL=https://wgenv.com
NODE_ENV=development

# File Upload Configuration
MAX_FILE_SIZE=10485760
MAX_FILES_COUNT=10

# Video Generation Settings
VIDEO_QUALITY=high
VIDEO_FORMAT=mp4
DEFAULT_VIDEO_DURATION=30

# Security Settings
# Development: Allow all origins
CORS_ORIGIN=*
# Production: Restrict to your domain
# CORS_ORIGIN=https://wgenv.com

# Directory Paths
UPLOAD_DIR=public/uploads
VIDEO_OUTPUT_DIR=public/videos

# Redis Configuration (Optional)
# For production, consider setting up Redis for better performance
# REDIS_URL=redis://localhost:6379
REDIS_DISABLED=true

# Production Security Settings (uncomment for production)
# FORCE_HTTPS=true

# Setup Instructions:
# 1. Copy this file to .env.local (development) or .env.production (production)
# 2. Update the BASE_URL and REACT_APP_BASE_URL to match your domain
# 3. For development: Frontend runs on VITE_PORT (3002), Backend on SERVER_PORT (5003)
# 4. For production: Point your domain DNS to your server IP
# 5. Run: npm run dev (development) or npm run build && npm start (production)